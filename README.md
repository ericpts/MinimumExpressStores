# Basic store example

### Prerequisites

From the project root:

`npm update`

### Server-Side
to start the server
``` bash
npm run dev
```

### Client-Side
to build the application
``` bash
cd app/views
rollup -c --watch
```
